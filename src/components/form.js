import { TextField } from '@mui/material';
import { Container } from '@mui/system';
import * as React from 'react';


const Form = ({props}) => {


    return(
        <Container>
            <TextField>
                ASD
            </TextField>
        </Container>
    );
}
export default Form;